[pytest]
# Run async tests without per-test decorators.
asyncio_mode = auto
# Recreate the event loop per test function (matches per-test DB isolation).
asyncio_default_fixture_loop_scope = function
# Add backend/ to sys.path so `app` and `api` packages import correctly.
pythonpath = .
testpaths = tests
